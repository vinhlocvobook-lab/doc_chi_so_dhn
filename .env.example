# ================================================================
# COPY file này thành .env và điền thông tin thực tế vào
# Không commit file .env lên Git!
# ================================================================

# --- Database Connection ---
# Luôn dùng 127.0.0.1 thay vì localhost để tránh lỗi Unix socket trên macOS/Linux
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password

# --- Application ---
# Bật true khi development, tắt false khi production
APP_DEBUG=true

# --- (Optional) App URL ---
# Bỏ comment và điền nếu cần override URL tự động
# APP_URL=http://localhost:8081
